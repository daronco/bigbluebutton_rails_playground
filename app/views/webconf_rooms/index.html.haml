= render_page_title t("_other.management.page_title")
= render :partial => "layouts/manage_menu"

= content_for :page_submenu do
  = render "webconf_servers/webconf_menu"

#manage
  = render "webconf_rooms/rooms_table", :rooms => @rooms
