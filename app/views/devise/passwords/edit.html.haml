= render_page_title t(".title")

.span6
  = simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f|
    = f.input :reset_password_token, :as => :hidden
    = f.full_error :reset_password_token
    = f.input :password, :label => t(".new_password"), :required => true, :hint => false
    = f.input :password_confirmation, :label => t(".confirm_new_password"), :required => true
    = f.button :wrapped, :value => t(".change_it"), :wrapper_class => "form-actions-clean"
.span6
  = render :partial => "devise/shared/links"
